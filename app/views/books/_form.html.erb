<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :book_name %>
    <%= form.text_field :book_name %>
  </div>

  <div class="field">
    <%= form.label :author_id %><br>
    <%= form.collection_select :author_id, Author.all, :id, :author_name %><br>
  </div>

    <div class="field">
    <%= form.label :library_id %><br>
    <%= form.collection_select :library_id, Library.all, :id, :library_name %>
  </div><br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

  
